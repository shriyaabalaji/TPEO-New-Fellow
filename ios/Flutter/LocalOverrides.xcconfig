# Bundle ID is applied by scripts/apply_local_bundle_id.sh into Runner.xcodeproj (not here).
# This file is optional; nothing includes it by default. If you add
#   #include? "LocalOverrides.xcconfig"
# to Debug.xcconfig / Release.xcconfig, you may set overrides below.
